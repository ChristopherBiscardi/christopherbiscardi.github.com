type Query {
  allDevTipTweets: [DevTipTweet!]
}

type DevTipTweet @collection(name: "dev-tip-tweets") {
  devTipId: String
  favoriteCount: Int
  retweetCount: Int
  tweetContent: String
  tweetCreatedAt: String
  tweetId: String
}
